"""Canonical reporting: `generator` produces/persists the one report an
investigation makes, `serializers` render it per channel. Neither computes
new evidence — rule #3, "every investigation produces exactly one
CanonicalReport; channels only render it differently."
"""
